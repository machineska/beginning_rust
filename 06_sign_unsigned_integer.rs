fn main() {
    let result = 20; // i32 by default
    let age:u32 = 30;
    let sum:i32 = 5-25;
    let mark:isize = 20;
    let count:usize = 40;
    println!("result value is {}", result);
    println!("sum {} and age {}", sum, age);
    println!("mark {} and count {}", mark, count);
}
